# RunPod pod dependencies.
# Pinned explicitly (not just relying on the template's preinstalled version):
# diffusers' dev branch (needed for QwenImage21Pipeline) and transformers>=5.17
# both require PyTorch >= 2.5 — older templates ship 2.4.x, which fails at import
# time with "infer_schema(func): Parameter q has unsupported type torch.Tensor".
torch>=2.5.1
# QwenImage21Pipeline is only in the diffusers development version (per the model card).
git+https://github.com/huggingface/diffusers
transformers>=5.17
accelerate
safetensors
sentencepiece
huggingface_hub
fastapi
uvicorn[standard]
python-multipart
pillow
